const { Server } = require("http");
const { json, arrayBuffer } = require("stream/consumers");

if (hour < 18) {
    greeting = "Good day";
  } else {
    greeting = "Good evening";
  }


  